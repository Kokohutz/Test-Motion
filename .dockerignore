# Keep the build context small: sample videos alone are ~150 MB and are
# not served (players upload their own videos in the browser).
*.mp4

# Legacy Python pipeline (replaced by the in-browser extractor)
*.py
requirements.txt
__pycache__

# Build artifacts and dependencies (recreated inside the image)
node_modules
dist
public/mediapipe-wasm

# Tooling / VCS
.git
.gitignore
.vscode
Dockerfile
docker-compose.yml
.dockerignore
